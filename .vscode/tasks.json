{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-frontend",
            "options": {
                "cwd": "${workspaceFolder:}/app/frontend"
            },
            "type": "shell",
            "command": "npm i && npm run build",
        }
    ]
}